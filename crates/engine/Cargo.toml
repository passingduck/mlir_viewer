[package]
name = "engine"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
tempfile = "3"
trace-format = { path = "../trace-format" }
